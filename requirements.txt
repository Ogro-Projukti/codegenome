# Compatibility entry point for development installs.
# Dependency declarations live in pyproject.toml; security-sensitive resolutions
# are fixed in constraints.txt so this install path cannot drift to vulnerable releases.
-c constraints.txt
-e .[dev]
